export const localEnvironment: {
  readonly lanApiBase: string;
  readonly publicApiBase: string;
  readonly buildMode: "development" | "production";
  readonly enableDevIdentityPreview: boolean;
  readonly enableDevFakeIdentityLogin: boolean;
};
