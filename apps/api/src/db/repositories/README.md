# Repositories

Repository interfaces and persistence implementations live here.

Current data-layer behavior must follow `docs/current/CURRENT_PROJECT_RULES.md`, including scan-token privacy, attachment metadata boundaries, and FileStorageAdapter-backed file access.
